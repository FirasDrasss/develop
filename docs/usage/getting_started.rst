Getting started with cooldata
==================================================
1. **Installation**: Install the `cooldata` library using pip:

.. code-block:: bash

   pip install cooldata

   
The PyVista dataset is the main format to use the `cooldata` library. It allows you to load and visualize the samples from the dataset in its original format, which is CGNS files.